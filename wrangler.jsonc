{
  // Cloudflare Workers config: serves the static Astro build as assets.
  // Deployed by Workers Builds with `npm run build` then `npx wrangler deploy`.
  "name": "juicy-burger-menu",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"
  }
}
